# Quick Start

1. Install Python 3.10+.
2. Install dependencies:
   `pip install -r requirements.txt`
3. Keep `app.py` and `Stroke_Diagnosis_model.pkl` in the same folder.
4. Run:
   `python app.py`

The original notebook is included for the complete data-preprocessing, training, tuning, and evaluation workflow.
